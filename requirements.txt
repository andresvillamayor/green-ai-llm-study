# GREEN-IA — Dependencias del proyecto
# Maestria Ciencia de Datos, Universidad Comunero, Paraguay
#
# Instalacion basica:
#   pip install -r requirements.txt
#
# llama-cpp-python requiere flags especiales segun el backend:
#
#   macOS (Metal):
#     CMAKE_ARGS="-DLLAMA_METAL=on" pip install llama-cpp-python --no-cache-dir
#
#   Windows (CUDA):
#     set CMAKE_ARGS=-DGGML_CUDA=on
#     pip install llama-cpp-python --no-cache-dir
#
# Verificar instalacion:
#   python scripts/validate_config.py --check-backend

codecarbon
llama-cpp-python
pandas
numpy
pyyaml
tqdm
psutil
anthropic
python-dotenv
matplotlib
scipy

# ─── Opcional: metricas GPU en Windows NVIDIA ────────────────────────────────
# Si pynvml no esta instalado, el experimento continua sin metricas GPU NVML.
# Solo requerido en hardware_profile=windows_nvidia con execution_device=gpu.
# Instalar manualmente si se usa Windows:
#   pip install pynvml
